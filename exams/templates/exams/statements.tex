{% load exams_filters %}

\documentclass[a4paper,11pt]{article}
\usepackage[margin=3cm]{geometry}
\usepackage{tabularx}
\usepackage{xepersian}


\settextfont{XB Zar}

\begin{document}
    \begin{tabularx}{\linewidth} {@{} r X @{}}
    {% for question in exam.question_set.all %}
        {{question.order}}) &
        \begin{minipage}[t]{\textwidth}
            {{question.statement}}
            \vspace{10pt}\\
            {\begin{tabularx}{\linewidth} {@{} X X X X X @{}}
            {% for choice in question.choice_set.all %}
                {{forloop.counter|choice_character}}) {{choice.choice}} {% if not forloop.last %} & {% else %} \\ {% endif %}
            {% endfor %}
            \end{tabularx}}
        \end{minipage}
        \vspace{15pt}\\
    {% endfor %}
    \end{tabularx}
\end{document}
